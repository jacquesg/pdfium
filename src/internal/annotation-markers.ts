/**
 * Internal annotation marker keys/values used by editor workflows.
 *
 * @module internal/annotation-markers
 * @internal
 */

/**
 * Dictionary key used to tag Ink annotations created by the editor line tool.
 */
export const LINE_FALLBACK_MARKER_KEY = '__pdfiumLineFallback';

/**
 * Marker value stored under {@link LINE_FALLBACK_MARKER_KEY}.
 */
export const LINE_FALLBACK_MARKER_VALUE = '1';
